# 实验，第一部分

## 认知实验报告

### 实验目的

* 认识 `Chroma 62000H` 型太阳光伏阵列模拟器的外形、功能；
* 学习使用 `Chroma 62000H` 型太阳光伏阵列模拟器的基本操作方法；
* 了解 `Chroma 62000H` 型太阳光伏阵列模拟器的编程基本操作，为第二部分编程模拟实验做准备。

### 实验器材

* 至少一部`Chroma 62000H` 型太阳光伏阵列模拟器；
* `200/220Vac` 或 `380/400Vac` 或 `440/480Vac` 交流电源输入；
* 一台运行 `Windows XP SP2` 或更高版本，且具有通用串行总线接口的计算机；
* 若干适用于 `Chroma 62000H` 型太阳光伏阵列模拟器的连接线材。

### 实验步骤

#### 准备工作

* 1. 取出 `Chroma 62000H` 型太阳光伏阵列模拟器，将其放置在实验场地适切位置，并妥善接地；

> 预期结果：无异常

* 2. 准备为模拟器供电的交流电源，这里使用一个参数为 `380/400Vac` 的交流电源。为了确保实验精度，将其误差校正在 `±0.01%` 以内；

> 预期结果：无异常

* 3. 将 `Chroma 62000H` 型太阳光伏阵列模拟器与准备好的电源相连，确认无误后按下位于前面板上的「启动」按钮（见 Figure 1-3 的按钮 17）。

> 预期结果：机器通电，进行自检通过后，进入模拟器主界面。

![Front Panel Image](https://raw.githubusercontent.com/yuetsin/62000H-notes/master/imgs/image.1.png)

#### 基础操作

> 主要进行一些初步的恒压源／恒流源配置。

##### 设定输出电压

* 4. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 5. 按下机器上的「VOLT」按钮（见 Figure 1-3 上的按钮 3）；
  
  > 预期结果：机器主页面（MAIN PAGE）上的 V CURSOR（电压游标）闪烁。

* 6. (a) 使用数字小键盘 （见 Figure 1-3 上的按钮 2）输入需要设定的电压值，并按下 ENTER 按钮确认（见 Figure 1-3 上的按钮 7）；
  
  > 预期结果：电子显示屏上显示出对应的电压数值。

* 6. (b) 使用旋钮来调整输出数据每一位的具体数值，配合使用上下左右调整按钮来在位之间移动。在获得需要的数值之后，并按下 ENTER 按钮确认（见 Figure 1-3 上的按钮 7）；
  
  > 预期结果：电子显示屏上显示出对应的电压数值。

* 7. 按下 ON/OFF 按钮（见 Figure 1-3 上的按钮 11）来应用刚刚的更改。

> 预期结果：机器输出所设定好的电压。

##### 设定输出电流

- 8. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

- 9. 按下机器上的「CURR」按钮（见 Figure 1-3 上的按钮 4）；
  
  >  预期结果：机器主页面（MAIN PAGE）上的 C CURSOR（电流游标）闪烁。

- 10. (a) 使用数字小键盘 （见 Figure 1-3 上的按钮 2）输入需要设定的电流值，并按下 ENTER 按钮确认（见 Figure 1-3 上的按钮 7）；
  
  > 预期结果：电子显示屏上显示出对应的电流数值。

- 10. (b) 使用旋钮来调整输出数据每一位的具体数值，配合使用上下左右调整按钮来在位之间移动。在获得需要的数值之后，并按下 ENTER 按钮确认（见 Figure 1-3 上的按钮 7）；
  
  > 预期结果：电子显示屏上显示出对应的电流数值。

- 11. 按下 ON/OFF 按钮（见 Figure 1-3 上的按钮 11）来应用刚刚的更改。

> 预期结果：机器输出所设定好的电流。

#### 使用 APG Interface

>  APG Interface，全称是 Analog Programming Interface，其功能主要为设置要求理想数值、以及测量实际数值。

* 15. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 16. 进入 System Config Setup Page（系统配置页面），依次按下「1」、「ENTER」按钮；
  
  > 预期结果：初始情况下，液晶屏幕显示如下图（Figure 3-5）所示。
  
  ![figure.3.5.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/04-18-05-00-figure.3.5.png)
  
  ##### 设定 APG 编程模式

* 17. 对 APG VSET、APG ISET、APG VMEAS、APG IMEAS 四项，选定后可通过 0、1、2、3、4 按钮或旋钮来在五种模式中切换，其中每种模式的含义如下：
      
      * NONE：意为不使用编程功能。
      
      * Vref（0V～5V）：意为使用外部电压源作为编程设定，且取值范围在 0 到 5V 之间。
      
      * Vref（0V～10V）：意为使用外部电压源作为编程设定，且取值范围在 0 到 10V 之间。
      
      * Iref（4mA～20mA）：意为使用外部电流源作为编程设定，且取值范围在 4mA 到 20mA 之间。
      
      * Rref（0～5kΩ）：意为使用外部阻抗作为编程设定，且取值范围在 0 到 5kΩ 之间。
  
  > 提示：其中四种编程模式的关系均为简单线性关系。即，DC SOURCE SET 的值将在给定的取值范围内与输入信号构成线性相关关系。
  
  > 注意：其中 APG VMEAS 模式和 APG IMEAS 模式不可应用第五种模式（即 Rref（0～5kΩ）模式）。
  
  > 注意：在超过取值范围数值被给定的情况下，对实际情况不做任何保证。

* 18. 使用上下光标选定 BUZZER 项之后，可将其值在 ON／OFF 之间切换。观察发生的现象。
  
  > 预期结果：
  > 
  > 在 BUZZER 项设定为 ON 的情况下，设备将会对用户对按钮和旋钮的操作给予蜂鸣音回馈，且在出现严重错误时长鸣（这也是默认设定）；
  > 
  > 在 BUZZER 项设定为 OFF 时，则任何情况下都不会发出蜂鸣音。

* 19. 使用上下光标选定 POWER ON STATUS 项之后，可将其值在 DEFAULT、LAST TURN OFF STATUS 和 USER DEFINITION 之间切换。多次设定电压电流值并重启设备，观察发生的现象。
  
  > 预期结果：
  > 
  > 在 POWER ON STATUS 设定为 DEFAULT 的时候，每次系统启动时都会将电压设定为 0.00V，电流设定为 0.00A，OUTPUT（输出源）设定为 OFF；
  > 
  > 在 POWER ON STATUS 设定为 LAST TURN OFF STATUS 的时候，系统将记住上次系统启动时的 V、I、OUTPUT 值，并将其保留到下次开机；
  > 
  > 在 POWER ON STATUS 设定为 USER DEFINITION 的时候，每次系统启动时都会将用户预设好的 V、I、OUTPUT 值填充进设定界面。

##### 设定电压墙

> 通过机器提供的 VOLTAGE LIMIT 设定项，我们可以限定机器所能输出的电压的上下限（即同时包括电压的最大值和最小值），通过预先配置电压数据的可用区间来提升设备实验中的安全性和可靠性。

* 20. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 21. 进入设备 SETTINGS 页，进入 VOLTAGE LIMIT SETTING 子菜单，并使用「上下左右」导航按钮到 V LIMIT：字段；
  
  > 预期结果：V LIMIT：字段的 MAX = 和 MIN = 子字段开始闪烁。

* 22. 在 V LIMIT：MAX 字段中填写预期的电压最大值；在 V LIMIT：MIN 字段中填写预期电压的最小值。
  
  > 注意：默认情况下，V LIMIT：MAX 会被配置为 100V，V LIMIT：MIN 会被配置为 20 V。
  
  > 注意：V LIMIT：MAX 字段值需要大于或等于 V LIMIT：MIN。

* 23. 按下 ENTER 按钮来确认更改，并按下 EXIT 按钮。
  
  > 预期结果：回到主页面（MAIN PAGE）。

##### 测试电压墙

* 24. 仿照 步骤 4～步骤 7 的方式，将机器的输出电压更改为超出电压墙限制范围的值；
  
  > 预期结果：机器主页面出现 V LIMIT RANGE 范围提示。若 BUZZER 项被设置为 ON，那么机器会发出一声短促的「Beep」作为警告。

* 25. 将机器输出电压退回正常范围内；
  
  > 预期结果：机器主页面的警告 Banner 消失；不再发出警告音。

##### 设定电流墙

> 通过机器提供的 CURRENT LIMIT 设定项，我们可以限定机器所能输出的电流的上下限（即同时包括电流的最大值和最小值），通过预先配置电流数据的可用区间来提升设备实验中的安全性和可靠性。

- 26. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；
- 27. 进入设备 SETTINGS 页，进入 CURRENT LIMIT SETTING 子菜单，并使用「上下左右」导航按钮到 I LIMIT：字段；
  
  > 预期结果：I LIMIT：字段的 MAX = 和 MIN = 子字段开始闪烁。

- 28. 在 I LIMIT：MAX 字段中填写预期的电流最大值；在 I LIMIT：MIN 字段中填写预期电流的最小值。
  
  > 注意：默认情况下，I LIMIT：MAX 会被配置为 20A，I LIMIT：MIN 会被配置为 2A。
  
  > 注意：I LIMIT：MAX 字段值需要大于或等于 I LIMIT：MIN。

- 29. 按下 ENTER 按钮来确认更改，并按下 EXIT 按钮。
  
  > 预期结果：回到主页面（MAIN PAGE）。

##### 测试电流墙

- 30. 仿照 步骤 8～步骤 11 的方式，将机器的输出电流更改为超出电流墙限制范围的值；
  
  > 预期结果：机器主页面出现 I LIMIT RANGE 范围提示。若 BUZZER 项被设置为 ON，那么机器会发出一声短促的「Beep」作为警告。

- 31. 将机器输出电流退回正常范围内；
  
  > 预期结果：机器主页面的警告 Banner 消失；不再发出警告音。

##### 设定电压摆率限制

> 所谓摆率，或称为转换速率（SLEW RATE），指的是在电子设备中，定义为每单位时间内电压或电流或任何其他电量的变化的物理量。以SI单位表示，测量单位一般为伏特/秒或安培/秒。 电子电路可以规定其输入或输出的转换速率的最小或最大限制。

> 为电压设定摆率上限是很有必要的，因为过于剧烈的电压变化很可能造成永久性的数字电路损坏。

* 32. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 33. 进入设备 SETTINGS 页，进入VOLTAGE SLEW RATE SETTING 子菜单，并使用「上下左右」导航按钮到 V SLEW RATE：字段；
  
  > 预期结果：V SLEW RATE：字段开始闪烁。

* 34. 使用数字小键盘或旋钮（ROTARY KNOB）来设定需要的 V SLEW RATE；
  
  > 注意：此处的 V SLEW RATE 单位是 V/mS（伏特每毫秒）。

* 35. 按下 ENTER 按钮来确认更改，并按下 EXIT 按钮。
  
  > 预期结果：机器主页面的警告 Banner 消失；不再发出警告音。

##### 测试电压摆率限制

* 36. 先决条件：先完成 1～3 步中的准备工作、机器自检（SELF-TEST）过程无异常，且已按照 32～35 步完成了电压摆率的配置；

* 37. 仿照 步骤 4～步骤 7 的方式，将机器的输出电压在两个相差较大的值之间切换（其间隔不超过 0.5 ms），并记录输出电压随时间变化的图线；
  
  > 注意：以 62150H-600 设备为例，其能处理的最大摆率为 10V/mS，其能分辨的最小摆率为 0.001V/mS。超过这个范围的电压摆率可能会引起误差，甚至设备损坏。
  
  > 预期结果：在设备所能处理的范围内，保证了输出电压的摆率未超过设定值；最终结果应与 Figure 3-21 类似。

![屏幕快照 2019-09-08 下午1.55.31.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/08-13-55-39-%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-09-08%20%E4%B8%8B%E5%8D%881.55.31.png)

##### 设定电流摆率限制

> 为电流设定摆率上限是很有必要的，因为过于剧烈的电流变化很可能造成永久性的数字电路损坏。

- 38. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；
- 39. 进入设备 SETTINGS 页，进入CURRENT SLEW RATE SETTING 子菜单，并使用「上下左右」导航按钮到 I SLEW RATE：字段；
  
  > 预期结果：I SLEW RATE：字段开始闪烁。

- 40. 使用数字小键盘或旋钮（ROTARY KNOB）来设定需要的 I SLEW RATE；
  
  > 注意：此处的 I SLEW RATE 单位是 A/ms（安培每毫秒）。
- 41. 按下 ENTER 按钮来确认更改，并按下 EXIT 按钮。
  
  > 预期结果：机器主页面的警告 Banner 消失；不再发出警告音。

##### 测试电流摆率限制

- 42. 先决条件：先完成 1～3 步中的准备工作、机器自检（SELF-TEST）过程无异常，且已按照 38～41步完成了电流摆率的配置；
- 43. 仿照 步骤 4～步骤 7 的方式，将机器的输出电流在两个相差较大的值之间切换（其间隔不超过 0.5 ms），并记录输出电流随时间变化的图线；
  
  > 注意：以 62150H-600 设备为例，其能处理的最大摆率为 0.1A/ms，其能分辨的最小摆率为 0.001A/ms。超过这个范围的电流摆率可能会引起误差，甚至设备损坏。
  
  > 预期结果：在设备所能处理的范围内，保证了输出电流的摆率未超过设定值；最终结果应与 Figure 3-23 类似。
  
  ![屏幕快照 2019-09-08 下午2.02.40.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/08-14-02-50-%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-09-08%20%E4%B8%8B%E5%8D%882.02.40.png)

![屏幕快照 2019-09-08 下午1.55.47.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/08-14-03-56-%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-09-08%20%E4%B8%8B%E5%8D%881.55.47.png)

> 步骤 20 ～ 43 的 OUTPUT SETUP 配置页面示意图（Figure 3-20）

#### 串并联操作实验

> 62000H 系列产品提供了串联和并联操作的能力。

> 注意：此段实验可能需要多台同型号的 Chroma 设备。

> 警告：在进行串并联操作的时候，务必遵从 Table 3-1 对最大设备数和最大电流／电压的限制说明；否则可能造成永久性设备损坏或／和人身损伤。

![屏幕快照 2019-09-08 下午2.20.01.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/08-14-20-10-%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-09-08%20%E4%B8%8B%E5%8D%882.20.01.png)

> 警告：不要混合使用串联和并联两种方法。

##### 串联设备操作实验

* 44. 准备数台同型号的 62000H 系列产品，并对每一台进行 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 45. 再次确认所进行的操作没有违背 Table 3-1 中的各种限制；

* 46. 以 62150H-600 为例，将两台设备的「VO- SVO-」接口和「SVO+ VO+」接口相连，构成一个串联电路，如图 Figure 3-27 所示；

* 47. 将两部设备的输出模式调整为「按电压输出」，且电压之和不超过 1200 伏特；
  
  > 注意：62150H-40 的最大串联电压和为 80 伏特；62150H-450 的最大串联电压和为 900 伏特；62150H-600 的最大串联电压和为 1200 伏特；62150H-1000 的最大串联电压和为 1200 伏特。

![屏幕快照 2019-09-08 下午2.23.34.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/08-14-23-42-%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-09-08%20%E4%B8%8B%E5%8D%882.23.34.png)

> 预期结果：DUT 测量到输出电压为两部设备的输出电压之和。

> 注意：串联设备的最大数目为 2。

##### 并联设备操作实验

- 48. 准备数台同型号的 62000H 系列产品，并对每一台进行 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；
- 49. 再次确认所进行的操作没有违背 Table 3-1 中的各种限制；
- 50. 以 62150H-600 为例，将五台设备的「VO- SVO-」接口全部连接到一个等势点；将五台设备的「SVO+ VO+」接口全部连接到另一个等势点，构成一个并联电路，如 Figure 3-28 所示；
- 51. 将五部设备的输出模式调整为「按电流输出」，且电流和不超过 250 安培；
  
  > 注意：62150H-40 的最大并联电流和为 3750 安培；62150H-450 的最大并联电流和为 340 安培；62150H-600 的最大并联电流和为 250 安培；62150H-1000 的最大并联电流和为 150 安培。

![屏幕快照 2019-09-08 下午2.27.01.png](https://raw.githubusercontent.com/yuetsin/private-image-repo/master/2019/09/08-14-27-07-%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-09-08%20%E4%B8%8B%E5%8D%882.27.01.png)



> 预期结果：DUT 测量到输出电流为五部设备的输出电流之和。

> 注意：并联设备的最大数目为 10。

#### 设备显示配置

> 一些关于设备显示信息方式的配置实验。

##### 亮度配置

* 52. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 53. 进入 DISPLAY SETUP 页面，使用「上下左右」按钮将光标设定到 BRIGHTNESS 字段上；
  
  > 预期结果：BRIGHTNESS 字段值开始闪烁。

* 54. 使用 0～2 号按钮或旋钮将 BRIGHTNESS 字段值在以下三项之中切换；
  
  > 可用的三个 BRIGHTNESS 枚举项：
  > 
  > 0 - BRIGHTNESS = HIGH，亮度高
  > 
  > 1 - BRIGHTNESS = NORMAL，亮度普通
  > 
  > 2 - BRIGHTNESS = DIMMED，亮度暗淡

* 55. 按下 ENTER 按钮来确认更改，并按下 EXIT 按钮。
  
  > 预期结果：VFD 显示屏的亮度改变了。

##### BANNER（警告横条）配置

* 56. 先决条件：先完成 1～3 步中的准备工作，且机器自检（SELF-TEST）过程无异常；

* 57. 进入 DISPLAY SETUP 页面，使用「上下左右」按钮将光标设定到 DISPLAY SELECTION 字段上；
  
  > 预期结果：DISPLAY SELECTION 字段值开始闪烁。

* 58. 使用 0～3 号按钮或旋钮将 DISPLAY SELECTION 字段值在以下四项中切换；
  
  > 可用的四个 DISPLAY SELECTION 枚举项：
  > 
  > 0 - NONE，不显示任何警告信息
  > 
  > 1 - V/I LIMIT，仅标明电流和电压的合法范围
  > 
  > 2 - V/I/P PROTECT，除了标明电流和电压的合法范围之外，还在电流或电压超限时显示警告
  > 
  > 3 - V/I SLEWRATE，标明电流和电压摆率的最大值


